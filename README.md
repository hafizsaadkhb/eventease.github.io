# EventEase

EventEase is a Blazor WebAssembly app for browsing events, viewing details, and registering.

## Run locally

```powershell
# From the EventEase folder
dotnet build .
dotnet run .
```

## Pages

- Event list: / or /events
- Event details: /events/{id}
- Registration: /registration/{id}
